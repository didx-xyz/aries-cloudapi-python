fullnameOverride: redis-insight

podAnnotations:
  sidecar.istio.io/proxyCPU: 10m

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: redis-insight.127.0.0.1.nip.io
      paths:
        - path: /
          pathType: Prefix
